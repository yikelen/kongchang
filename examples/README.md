# 示例工程

## `demo_show.json`（文字演示）

- **不依赖**任何 mp3/mp4，可直接导入查看节目单结构。
- 在控场中：**工程 → 打开 / 导入…** → 选本文件。
- 正式使用时，把备注条改成 `audio` / `video`，并填写相对路径即可，例如：

```text
examples/
  demo_show.json
  audio/
    warmup.mp3
  video/
    opening.mp4
```

媒体文件请自行准备；请勿把含真实人名、客户名单的活动工程推到公开仓库。
